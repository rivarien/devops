version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:14.0
    steps:
      - checkout
      - run: npm install
      - run: npm install --only=dev
      - run: npm test
workflows:
  version: 2
  build-and-test:
    jobs:
      - build